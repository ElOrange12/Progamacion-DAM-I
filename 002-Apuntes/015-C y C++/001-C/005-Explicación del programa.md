Primero importo todas las librerias que necesite
stdio = standard input and output

```c
#include <stdio.h>
```

Tiene que haber un punto de entrada de la ejecución - obligatorio
Y la función de entrada se tiene que llamar main

C es un lenguaje fuertemente tipificado
int = devuelve un numero entero

```c
int main(){
	printf("Hola mundo desde C \n");
	return 1;
}
```

Función de impresión/salida por consola
printf("Hola mundo desde C \n");

Si te has comprometido a devolver un int, tienes que devolver un int
return 1;
